
<form method={{method}} action={{action}}>
    <p>Name: <input type="text" name="name" value="{{group.name}}">
    {{#if group}}
    {{group.name}}
    {{/if}}
    </p>
    <p>Description: <input type="text" name="description" value="{{group.description}}">
    {{#if group}}
    {{group.description}}
    {{/if}}
    </p>
    <input type="submit" value="{{btnAction}} Group">
</form>